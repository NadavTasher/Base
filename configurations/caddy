# Server settings
{
    admin off
    auto_https off
}

# Listen on port 80
:80

# Match API requests
reverse_proxy /api/* {
    to :8000
}

# Match static requests
file_server /* {
    root /app/frontend
}